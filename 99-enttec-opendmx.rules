# Enttec Open DMX USB — grant current session user access via libftdi1/libusb
# (libftdi bypasses the ftdi_sio kernel driver and accesses the chip directly)
SUBSYSTEM=="usb", ATTRS{idVendor}=="0403", ATTRS{idProduct}=="6001", TAG+="uaccess", MODE="0664"
